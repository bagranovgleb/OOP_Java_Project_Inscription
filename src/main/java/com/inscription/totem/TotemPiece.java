package com.inscription.totem;

/** A totem piece held in inventory - either a TotemHead (defines a tribe) or a TotemBody (carries a sigil). */
public interface TotemPiece {

    String describe();
}
